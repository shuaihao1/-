<html>
    <head>
            <script>

                    /*
                        for
                           1. var i = i 初始值
                           2. i <=100 循环条件，满足条件才可以进入循环
                           3. 执行循环体。document。write（i+"br/>");
                           4. 执行i++ ， 循环修正条件
                           5. ...234 重复...
                           6. 当i为101时退出循环

                    */
                    /*  
                        for (var i =1; i<=10;i++){
                        document.write(i+"<br/>");
                    }
                    */

                        /*
                            中间条件不写，死循环，占据大量内存。

                        */
                        for (var i =1; ;i++){
                            document.write(i+ "<br>");
                        }
                        /*   var i=0
                              for (; i <100; i+1){
                            document.write(i + "<br>");
                        }
                        */
                        /* for(var i = 0 ; i<100;){
                            document.write(i+"<br>");
                            i++
                        }
                        */var
                        
                        /*
                            var i = 0;
                            for( ; ;){
                            if(i== 100){
                                break;
                            }
                            document.write(i+"<br>");
                            i++;
                        }
                        */

                        /*
                       
                        for( var i =0; i<100; i++){             //使用var定义的，虽然在循环内部，但是外部依旧可以访问
                            console.log(i); 
                        }
                        console.log(i); //100

                        */
                        /*

                        for(let j =0; j <100; j++){    // 使用let定义的变量j,  j 的作用范围在for循环之内。超出不起作用。
                            console.log(j);
                        }4
                        console.log(j); //j is not defined    j 没有被定义

                        */

                        /*
                        for( var i = o ; i<100; i+2){       //每次修正的条件 2为 步长
                            console.log(i)
                        }
                        */

                        /* for(i&7==0)
                            var gewei =i%10;
                            var shiwei =parseInt(i&10);
                            console.log("个位：+gewei");
                            console.log("十位：+shiwei");
                        */
                            
                        
                        for (var i = 0; i<100; i++){
                            if(i %7 ==0){
                                console.log("我跳过了"+i);
                                //continue;  //跳过当前的一轮的循环，继续后面的循环
                                break;   //中断整个循环，后面的循环次数不再执行
                            }
                            console.log(i)
                        }

            </script>
    </head>
    <body>




        
    </body>
</html>
